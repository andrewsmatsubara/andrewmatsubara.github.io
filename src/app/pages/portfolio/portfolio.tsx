import styles from './portfolio.module.scss';

export default function Portfolio() {
  return (
    <section className={styles['portfolio']} id='portfolio'>
      <p>Nothing to see here yet... but working on it!</p>
    </section>
  ); 
}
